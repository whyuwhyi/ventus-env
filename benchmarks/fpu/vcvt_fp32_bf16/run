#!/usr/bin/env bash
set -euo pipefail
ROOT=$(cd "$(dirname "$0")/../../.." && pwd)
source "$ROOT/env.sh"
mkdir -p .tmp
export TMPDIR="$(pwd)/.tmp"
export POCL_TEMP_DIR="$(pwd)/.tmp"
exec "./$(basename "$PWD").out"
